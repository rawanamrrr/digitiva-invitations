-- Run this in Supabase SQL editor to create an admin user
-- Replace with your desired email and run: node -e "const bcrypt=require('bcryptjs');bcrypt.hash('yourpassword',12).then(console.log)"
-- Then paste the hash below

-- INSERT INTO users (name, email, password_hash, role) VALUES
-- ('Admin', 'admin@example.com', 'REPLACE_WITH_BCRYPT_HASH', 'admin');
